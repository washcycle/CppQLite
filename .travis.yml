language: cpp

compiler:
    - clang
    - gcc

sudo: required

before_script:
    - sudo add-apt-repository ppa:andykimpe/cmake3 -y
    - sudo apt-get update -q
    - sudo apt-get install cmake3

#addons:
#  apt:
#    sources:
#    - andykimpe

#addons:
#  apt:
#    packages:
#    - cmake

before_install:
    - cmake -DBUILD_TEST=1